{
  "name": "mips-checksum",
  "version": "1.0.0",
  "scripts": {
    "install:all": "npm --prefix backend install && npm --prefix frontend install",
    "start": "npx concurrently -n BACKEND,FRONTEND -c cyan,magenta \"npm --prefix backend start\" \"npm --prefix frontend run dev\"",
    "build:frontend": "npm --prefix frontend run build"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  },
  "dependencies": {
    "clsx": "^2.1.1",
    "react-fast-marquee": "^1.6.5",
    "tailwind-merge": "^3.5.0"
  }
}
